# Gradle settings
org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.daemon=false

# Isolate gradle cache to project directory
systemProp.gradle.user.home=./.gradle_repo

# ============ Environment Properties ============
minecraft_version=1.20.1
minecraft_version_range=[1.20,1.20.5)
forge_version=47.2.0
forge_version_range=[46,)
loader_version_range=[46,)

# Mappings: official (no extra plugins needed)
mapping_channel=official
mapping_version=1.20.1

# ============ Mod Properties ============
mod_id=jett
mod_name=Just Enough Tooltips
mod_license=MIT
mod_version=1.5.0-Forge-1.20-1.20.4
mod_group_id=com.shao.jett
mod_authors=Shao_Zhige
mod_description=Enough your tooltips.
